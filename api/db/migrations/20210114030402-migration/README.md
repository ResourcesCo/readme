# Migration `20210114030402-migration`

This migration has been generated by Benjamin Atkin at 1/13/2021, 10:04:02 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "User" (
"id" SERIAL,
    "username" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "authId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Project" (
"id" SERIAL,
    "name" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "readme" TEXT NOT NULL,
    "private" BOOLEAN NOT NULL,
    "userId" INTEGER,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User_username_unique_constraint" ON "User"("username")

CREATE INDEX "User.username_index" ON "User"("username")

CREATE INDEX "User.email_index" ON "User"("email")

CREATE INDEX "User.authId_index" ON "User"("authId")

CREATE INDEX "Project.slug_index" ON "Project"("slug")

ALTER TABLE "Project" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20210114030402-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,36 @@
+datasource DS {
+  // optionally set multiple providers
+  // example: provider = ["sqlite", "postgresql"]
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = "native"
+}
+
+model User {
+  id       Int       @id @default(autoincrement())
+  username String
+  email    String
+  authId   String
+  projects Project[]
+
+  @@unique([username], name: "User_username_unique_constraint")
+  @@index([username])
+  @@index([email])
+  @@index([authId])
+}
+
+model Project {
+  id      Int     @id @default(autoincrement())
+  name    String
+  slug    String
+  readme  String
+  private Boolean
+
+  @@index([slug])
+  User   User? @relation(fields: [userId], references: [id])
+  userId Int?
+}
```


