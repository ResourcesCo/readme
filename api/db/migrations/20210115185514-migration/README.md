# Migration `20210115185514-migration`

This migration has been generated by Benjamin Atkin at 1/15/2021, 1:55:14 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "Project" DROP CONSTRAINT "Project_userId_fkey"

CREATE TABLE "Readme" (
"id" SERIAL,
    "name" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "body" TEXT NOT NULL,
    "private" BOOLEAN NOT NULL,
    "userId" INTEGER,

    PRIMARY KEY ("id")
)

DROP TABLE "Project"

CREATE INDEX "Readme.slug_index" ON "Readme"("slug")

ALTER TABLE "Readme" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210114030402-migration..20210115185514-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,36 +1,36 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
   binaryTargets = "native"
 }
 model User {
-  id       Int       @id @default(autoincrement())
+  id       Int      @id @default(autoincrement())
   username String
   email    String
   authId   String
-  projects Project[]
+  readmes  Readme[]
   @@unique([username], name: "User_username_unique_constraint")
   @@index([username])
   @@index([email])
   @@index([authId])
 }
-model Project {
+model Readme {
   id      Int     @id @default(autoincrement())
   name    String
   slug    String
-  readme  String
+  body    String
   private Boolean
-  @@index([slug])
   User   User? @relation(fields: [userId], references: [id])
   userId Int?
+  @@index([slug])
 }
```


